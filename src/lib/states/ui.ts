import { writable } from "svelte/store";

export const tipsAndTricks = writable(true);
export const helperUI = writable(true);
export const explorerVisible = writable(false);
export const toolbarVisible = writable(true);
